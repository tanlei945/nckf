<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.benben.modules.business.coupons.mapper.CouponsMapper">
    <select id="getCouponsCount" resultType="int">
        SELECT COUNT(*) FROM `bb_user_coupons` where user_id=#{userId};
    </select>
</mapper>